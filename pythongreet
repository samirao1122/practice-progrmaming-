def disp(y):
    def greet():
        return "greet function"
    
    print("disp function")
    b = greet()
    print(b)
    return "disp function execution complete"

a = disp("hello")
print(a)

 namespace std;
int  sum()
{
int a;
int b;
  cin>>a;
  cin>>b;
  int sum;
  sum=a+b;
   return sum;

  
}
int main ()
{ int answer;
 while (true)
   {  

  
 
answer=sum();
 cout<<answer;}
  return 0;







  
}
